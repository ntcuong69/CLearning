import React from "react";
import { Paper } from "@mui/material";

export default function StudyplanManagement() {
  // Placeholder: Sẽ phát triển sau
  return (
    <Paper sx={{ p: 4, textAlign: "center", color: "#888" }}>
      (Bảng quản lý khóa học sẽ hiển thị ở đây)
    </Paper>
  );
} 